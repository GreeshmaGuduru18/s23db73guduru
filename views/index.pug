extends layout

block content
  h1= title
  p Welcome to Greeshma Gurudu application page #{title}

  button(type="button", class="btn btn-danger" onClick="goFaster()") Press  me
  br
  br
  div(id="faster") Increase the speed
  br
  script.

    var mph=20;
    var change=1;
    function goFaster() {
      mph =mph + change ;
      change =change +1;
      document.getElementById("faster").innerHTML=`MPH ${mph} with change ${change}.`;
    }
    hr
  a(href='/movies') Movies_Link